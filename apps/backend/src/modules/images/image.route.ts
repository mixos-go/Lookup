import type { FastifyInstance } from 'fastify';

export async function imageRoutes(app: FastifyInstance) {
  // TODO: POST /api/images/upload — see docs/API.md #Module:Images
  // TODO: PATCH /api/products/:productId/images
}
